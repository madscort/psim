project: "psim"
accelerator: "gpu"
devices: -1
dataset: "dataset_v01"
batch_size: 64
num_workers: 4
epochs: 100
run_group: transformer_alldb
run_name: transformer

defaults:
  - model: transformer_profile
  - _self_

optimizer:
  name: "adamw"
  lr: 0.0001
  warmup: True
